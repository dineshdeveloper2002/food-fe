name: workflow-trigger
on:
  push:
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
    - name: log check
      run: 'echo print hello world'
 build:
   runs-on: ubuntu-latest
   steps:
     - name: checkout code
       uses: actions/checkout@v4
  
  
    
      
    
